apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: s18share-ingest-worker-redis
  namespace: s18share
spec:
  scaleTargetRef:
    name: s18share-ingest-worker
  pollingInterval: 15
  cooldownPeriod: 120
  minReplicaCount: 1
  maxReplicaCount: 4
  triggers:
    - type: redis
      metadata:
        address: s18share-redis.s18share.svc.cluster.local:6379
        listName: ingest
        listLength: "2"
        databaseIndex: "0"
      # If Redis requires auth, copy keda-triggerauth.template.yaml to
      # keda-triggerauth.yaml, set the password secret, apply it, then uncomment:
      # authenticationRef:
      #   name: s18share-redis-auth
