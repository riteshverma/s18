[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    integration: requires external services (HTTP, subprocess, live backend)
    slow: runtime > 2s or wall-clock heavy
    contract: cross-repo integration boundary (wise-ai, gbrain, mcp)
